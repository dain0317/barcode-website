import React from "react";

const Logo = () => {
  return (
    <img
      src={require("../imgs/Barcode-simpleLogo.png")}
      alt="logo"
      className="main-logo"
    />
  );
};

export default Logo;
